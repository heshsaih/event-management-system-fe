import { Typography } from "@mui/material";
import StyledContainer from "../../../components/StyledContainer";

export default function UsersPage() {
  return (
    <StyledContainer>
      <Typography variant="h3">Panel zarządzania użytkownikami</Typography>?
    </StyledContainer>
  );
}
